/**
 * ⚠️ GENERATED PACKAGE - MANUEL EDİT YASAK
 *
 * Bu içerik `packages/contracts` kaynaklı codegen için yer tutucudur.
 */
import type { Booking, BookingCreate, User } from '@project/contracts';

export type HealthResponse = {
  status: 'ok';
  version: string;
  environment: string;
};

export type BookingResponse = Booking;
export type BookingCreateRequest = BookingCreate;
export type UserResponse = User;
